export default function getCookieArray() {
  /// 参照したいcookieのキー名を格納
  const key = 'id';

  /// 正規表現でcookie値を取得
//   const value = document.cookie.match(
// //     new RegExp(key + '=([^;]*);*')
// //   )[1];

//   /// cookie値を表示
//   console.log(value);
}
